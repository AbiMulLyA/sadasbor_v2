import 'package:flutter/material.dart';

class PresensiCardComponent extends StatelessWidget {
const PresensiCardComponent({Key? key}) : super(key: key);

@override
Widget build(BuildContext context) {
return Container();
}
}
